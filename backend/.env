# Configuracion de conexion a PostgreSQL
# Si "IN5AV" no resuelve en tu red/maquina, cambia PGHOST por "localhost"
PGHOST=localhost
PGUSER=IN5AV
PGPASSWORD=admin
PGDATABASE=clientes_db
PGPORT=5432

# Puerto del servidor backend
PORT=3000
